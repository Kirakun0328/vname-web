# ぶいネーム

VTuber・AIVTuberの名前を入力して、名前・読み・英字表記の重複候補を調べるツールです。

## ツールを開く

[ぶいネームを開く](https://kirakun0328.github.io/vname-web/)


## 使い方

名前を入力して「名前を調べる」を押すだけ。検索はブラウザ内で行います。入力内容は外部へ送信しません。

## 辞書について

初回拡充時点24,472レコード。AIVナビ連携後は24,603レコード（2026-09-07時点）。最新件数はサイト上に表示します。2026-09-07収集の既存辞書を使用しています。個人確認済みの人数ではなく、グループやチャンネルも含まれる可能性があります。過去の機械変換データは残っていますが、画面・検索では出典のない読みと英字を使いません。一致なしは名前の未使用を保証しません。

出典： [ユーザーローカル](https://virtual-youtuber.userlocal.jp/document/ranking)・[VTuber Database](https://vdb.vtbs.moe/)・[VTuber Post](https://vtuber-post.com/ranking_index.html)。項目抽出・名寄せ・機械変換を実施したデータです。一部ページは取得できていません。追加データは毎日自動更新します。

このリポジトリは公開画面用です。JavaScriptと辞書は閲覧・取得できます。検索結果はテキストとして表示し、CSPで外部接続とインラインスクリプトを制限しています。安全性を保証するものではありません。

## 読みの補正

`readings.js` の完全な名前単位の補正を、機械変換した読み・英字より優先します。空白の有無と全角・半角の違いを吸収し、姓だけを他の人へ置換することはありません。癒色えも（いしきえも）を含む13名を個別補正しています。未確認の読みは「不明」と表示し、誤読で検索に一致しないようにしています。

補正出典：[ホロライブ公式タレント一覧](https://hololive.hololivepro.com/talents/)（2026-09-07確認）。

## 多言語・別名検索

画面は日本語・英語・中文（簡体字）・한국어に対応。右上で切り替え、選択は端末内に保存します。初回はブラウザの言語に合わせます。検索入力と結果・ページ位置は言語切り替え時に維持します。

`extra-data.js` は既存711レコードに845個の別名・別言語表記を追加し、ユーザーローカルの公開ランキング1〜10ページから206レコードを追加しています。取得日：2026-09-07。未確認の読み・英字は推測せず空欄（画面では「不明」）にしています。初期データでは収集元IDと名前による重複候補の除外を行っていました。拡充版の新規登録はYouTubeチャンネルIDと出典のアカウント対応で照合し、同名の別チャンネルを名前だけで統合しません。異なる収集元の同一人物を完全には照合できていません。人数ではなくレコード数です。

## 自動更新

GitHub Actions が毎日日本時間6:17ごろに更新します。VTuber Databaseで既存レコードの別名を補完し、VTuber Postは小規模チャンネル側から1日90ページずつ、全ランキングページを順番に巡回します。登録者数の下限はありません。取得に失敗したページは後日再試行します。連続した取得失敗時は収集を早めに中断し、検証できたページを保存して次回再開します。GitHub側の都合で開始が遅れる場合があります。既存データと個別の読み補正を保持します。全員を毎日確認するものではありません。ユーザーローカル由来の既存データは維持し、名前の類似だけによる新規登録・除外は行いません。

取得失敗、形式変更、異常な件数増加時は更新を中止し、公開済みの辞書を維持します。Actions の「Update dictionary and publish」→「Run workflow」で手動実行も可能です。追加APIキーは不要です。公開リポジトリの定期実行は60日間リポジトリの活動がない場合にGitHub側で無効になることがあるため、停止時はActionsから再有効化してください。

## 読みの取得と優先順位

1. 出典付きの個別補正（本人・所属先の確認済み情報）
2. にじさんじ公式プロフィールの `ruby` フィールド
3. VTuber Postのプロフィール説明文にある、対象者のフルネームと読みを伴う自己紹介（例：名前（よみ）と申します）

名前の漢字から読みを推測したり、ローマ字から日本語の読みを逆算したりしません。括弧内がニックネームの可能性があるため、単なる括弧書きだけでは採用しません。出典URLを保存し、画面から確認できます。情報源の記載自体が正しいことを保証するものではありません。

毎日の更新では未確認・確認日が古いものから、公式プロフィール最大20件、VTuber Postプロフィール最大30件を順次確認します。全レコードを即時確認するものではありません。取得失敗時・明記がない場合は既存の出典付きの読みを保持し、個別補正は常に最優先します。

癒色えもの出典：[本人の自己紹介](https://note.com/ishiki_emo/n/n86e46e0ba925)。

## VTuber / AIVTuberタグ

AIVTuberも登録対象です。[毛玉雀のAIVナビ](https://aiv.nyagsicapp.com/characters)の公開キャラクター一覧を毎日の更新で取得します。YouTubeチャンネルIDと既存の収集元IDで照合し、既存レコードにAIVTuberタグと出典を追加、未収録の項目は新規登録します。同名という理由だけでは統合しません。AIVナビに明記された `name_kana` も、既存の出典付きの読みを上書きせず取得します。

AIVTuberタグがないレコードは広義のVTuberとして表示します。AI不使用を調査・確認したという意味ではありません。タグを押すと掲載元を確認できます。AIVナビ取得失敗時は既存のタグ・読み・登録を維持します。

画面下部にGitHubと[開発者のX](https://x.com/Kiratchi0328)へのリンクがあります。

## 掲載対象と小規模層への拡充

掲載対象は、実際にVTuber／AIVTuberとして活動を開始している方です。活動開始前の「VTuber準備中」の方は対象外です。サイト上にも日本語・英語・中国語・韓国語で明記しています。2016年以降の初期の活動者も収集対象です。引退・休止した方も、活動実績が確認できれば対象です。現在も活動中であることを掲載条件にはしていません。

新しいVTuber Postの登録には、VTuberディレクトリへの掲載と動画総再生回数が正であることを最低限の活動の手掛かりとし、準備中と明記された名前・再生実績不明の項目は追加しません。登録者数だけでは活動開始と判定しません。これは個々の初配信を視聴した証明ではありません。VDBのアカウント掲載だけで未確認の新規項目を追加しません。AIVナビの新規項目は活動中・配信中などの記載があるものを追加します。既存レコードの活動状況の全件確認は未完了で、元情報の古さ・漏れによる判定の限界があります。準備中の表示は取得元の更新で活動を確認できれば再掲載できます。

`python scripts/update_dictionary.py --full` でVTuber Postの全ページを巡回できます。POSTフォームによるページ送り、実際の順位・ページ件数・チャンネルIDの検証を行い、誤って先頭ページを何度も集めることを防ぎます。`scripts/collection-report.json` に取得ページ数・失敗ページ・新規件数・登録者1,000人未満の新規件数を保存します。失敗時は既存データを維持します。出典URLを検索結果から開けます。

「6万人以上」はVTuber全体に関する推計と、本ツールの収録件数を混同しないよう扱います。全VTuberの網羅や、6万人分の登録を保証するものではありません。

## VSTATSの個人勢一覧

[VSTATSの個人勢一覧](https://www.vstats.jp/brands/1)も収集元に追加しています。YouTubeチャンネルIDで既存レコードと照合し、プロフィールのチャンネルID一致・総再生回数を確認できた項目を追加します。準備中の表示と活動実績を確認できない項目は新規追加しません。毎日の更新では、未収録の小規模チャンネルを優先して最大30プロフィールを確認します。名前の読みはこの情報から推測しません。

## 小規模な活動者のプロフィール

[liverfun.jpのVTuber名鑑](https://www.liverfun.jp/vtuber/)から、合計SNSフォロワーが1,000未満のプロフィールを1日30件ずつ巡回します。この値はYouTube登録者数ではありません。本人プロフィールのVTuberとしての記載と、配信中・配信しています等の活動の明記を両方確認します。単なるVTuberファン、準備中、年が不明なデビュー予告、活動予定だけの項目は追加しません。公開プロフィール上の記載による確認であり、実際の配信映像を確認した保証ではありません。

掲載元のプロフィールURLと公開活動アカウントを保持し、同一アカウントは既存項目にまとめます。名前しか一致しない場合は、この情報源から新たな項目や人物同一性を推測しません。読みは推測しません。プロフィール本文・画像・連絡先は保存しません。

## 海外・過去の公開名鑑

- [タイVTuberランキング](https://vtuber.chuysan.com/)のフロントエンドが利用する[公開名簿JSON](https://storage.googleapis.com/thaivtuberranking.appspot.com/v2/channel_data/simple_list.json)を毎日照合します。準備中の表記を除き、過去の動画公開日時と正の総再生回数の両方があるチャンネルを対象にします。チャンネル作成日はデビュー日として扱いません。
- Eka Setyo Agung氏の[Indonesian Vtuber Channel Data](https://www.kaggle.com/datasets/ekasetyoagung/indonesian-vtuber-channel-data)（2024年6月13日・バージョン1、Apache-2.0）を利用します。名前・チャンネルID・活動を示す集計事実だけを取り込み、説明全文や画像は転載しません。本人のチャンネル名または説明にVTuber等の表記があり、公開動画数と総再生回数が正の項目に限定します。デビュー前の表記、切り抜き名義、タグしか根拠のない項目は除外します。
- [学術系Vtuber名鑑](https://scholarvtuber.com/)は毎日20プロフィールずつ巡回します。本人向けのチャンネルリンクと紹介動画の投稿者が一致し、動画が過去に公開され再生記録がある場合に追加します。他人のチャンネルのコラボ動画、予告のみ、準備中は追加しません。失敗が続く場合やアクセス制限を受けた場合は停止し、既存データを保持します。
- [AI VTuberDB](https://aivtuberdb.com/)の配信中キャラクターと個別確認した本人プロフィールは `scripts/reviewed-profiles.json` に出典付きで記録します。同じTwitch配信に登場する別キャラクターや人間の配信者を、同一人物の別名として統合しません。読みと英字表記は本人の明記があるものだけ登録します。

- [TaiwanVTuberData](https://taiwanvtuberdata.github.io/) の[公開JSON](https://github.com/TaiwanVtuberData/TaiwanVTuberTrackingDataJson)を一括取得します。`active` / `graduate` のうち、過去のデビュー日または公開動画の記載があるプロフィールが対象です。`preparing`、未来のデビュー日、活動実績が未確認のプロフィールは追加しません。YouTube登録者1,000未満やTwitchフォロワー1,000未満も収集します。
- [HoloListの2024年9月14日保存データ](https://github.com/xoltia/vtuber-database/releases/tag/2024-09-14)から、その時点ですでにデビューしていた活動者を追加します。引退・休止・活動停止も対象です。
- [2019年10月27日の公開チャンネル保存データ](https://github.com/Imamachi-n/Virtual-Youtuber-API/tree/cfa62639e60af5904b4faf5ae5d9a1702a80c4a7/Batch/data)を、VTuber本人としての自己紹介・投稿動画数・再生回数で照合します。切り抜き、準備中、活動実績を確かめられないチャンネルは新規追加しません。チャンネル開設日はVTuberデビュー日として扱いません。

公開アカウントIDで照合し、同じ活動者の別名は既存レコードへ追記します。共通の一括データURLは本人識別に使いません。読みは推測せず、確認済みの読みを維持します。保存データには `source_snapshot_at` と `snapshot_source` を記録し、現在の活動状況とは区別します。人物紹介文や連絡先、画像は転載しません。

[KAI-YOUの「VTuber統計レポート2024」解説](https://premium.kai-you.net/article/894)（2025年2月20日）には、Xアカウントを持つVTuberを主な調査対象として6万名以上という集計が記載されています。公開部分に全員の名簿はありません。この規模を収集目標とし、実際の掲載件数はブラウザーの辞書件数と `scripts/collection-report.json` で確認します。記事の推計値を収録件数として表示することはありません。

## YouTube以外のVTuber・Vライバーと活動媒体

掲載対象は配信媒体を問わず、活動開始済みのVTuber・AIVTuber・Vライバーです。
YouTubeのチャンネルIDは必須ではありません。引退・休止者も過去の活動実績があれば対象に含み、準備中・未デビューの人は掲載しません。

検索結果には以下を表示します。

- **主な活動媒体**: 本人・所属先が明記した配信先。複数媒体に対応し、出典を表示します。
- **確認できた媒体**: 主な活動媒体が不明な場合の、確認できたアカウント・活動情報。アカウントがあるだけで主な媒体とは判定しません。
- **配信先リンク**: 個人の公開アカウントへのリンク。確認できないURLや読みは生成しません。

媒体名の表示はYouTube、TikTok LIVE、IRIAM、Avvy、REALITY、Twitch、17LIVE、SHOWROOM、ツイキャス、ニコニコ、Mirrativ、bilibili、Spoon、Kick、SOOP、topia、Palmu、ミクチャ、BIGO LIVE、AcFunに対応します。表示対応と各媒体の収集完了は別です。すべての活動者を収集済みではありません。

`platform-data.js` は出典を持つ媒体メタデータとYouTube以外の新規掲載を保持します。既存の `data.js` / `extra-data.js` に対して `source_id` で重ね、既存の名前・読み・AIVTuber分類を保ちます。公開アカウントが一意に一致した場合だけ既存人物を補完し、同名だけでは統合しません。同じ配信に登場する複数のAIキャラクターも統合しません。

追加収集元:

- [321公式Vライバー一覧](https://vliver.321.inc/liver/): 活躍中と明記された一覧から個別プロフィールを取得し、「配信アカウント」欄を主な活動媒体として取り込みます。SNS欄は別扱いです。
- [Clover公式プロフィール](https://clover-live.com/liver-page/): 実際の配信・活動の記述と配信先を確認できたプロフィールのみ。
- [Avvy配信者インタビュー](https://panora.tokyo/archives/137121): 2026年4月27日のイベント入賞者15名の活動記録。Avvyでの活動を確認した記録として扱い、イベント参加だけで主な媒体とは判定しません。
- [VTuber Database](https://vdb.vtbs.moe/): 既存の掲載者についてのみ公式アカウントを補完。新規人物の活動開始判定には使用しません。

```sh
python scripts/update_platform_dictionary.py         # 各事務所40件ずつ、巡回して更新
python scripts/update_platform_dictionary.py --full  # 一覧全件（初回収集・手動調査用）
python -m unittest discover -s tests
node --test tests/test_platforms.cjs
```

日次更新は既存のGitHub Actionsで実行します。取得失敗時も既存レコードを保持し、失敗URLを次回に再試行します。収集結果と次回位置は `scripts/platform-report.json` に保存します。コード更新のpushでは検証済みデータを公開し、外部サイトの全巡回は日次または手動起動で行います。

今後ほかの媒体を追加するときは `scripts/platform_sources.py` と `platforms.js` の媒体対応を追加し、個人プロフィールのURLを正規化してください。アカウント作成・フォロワー数だけで活動開始や主な活動媒体を推測せず、本人や所属先の公開情報を `activity_source` / `primary_platform_source` に記録します。

## AIに名前相談・名前の傾向

「名前をチェック」「AIに名前相談」「名前の傾向」のタブを切り替えて使えます。スマートフォン向けの可変レイアウトを含みます。

**名前の傾向**は、ブラウザが現在の辞書から文字数、文字種、漢字と隣接する漢字2文字の出現レコード数を集計します。名前内の同じ文字は1回と数え、NFKC正規化後のUnicodeコードポイント数を使い、空白を除きます。確認できた活動媒体で絞り込めます。複数媒体の掲載者は各媒体に含まれ、全媒体では1レコードです。収集元・言語の偏りがあり、グループやチャンネル名も含むため、VTuber全体の人気や時系列の流行を示す統計ではありません。検索・分析にはAIのダウンロードもWebGPUも不要です。

**AI相談**は [Google LiteRT-LM Web API](https://developers.google.com/edge/litert-lm/js) と Gemma 4 E2B を使う試験機能です。モチーフや雰囲気から候補・読み・理由を提案し、各候補の表示名・読みを実際のローカル辞書で照合します。AIが考えた読みを確認済みの辞書データとして保存することはありません。読みや同名の一致がなくても、未使用の保証ではありません。

- GitHub PagesはHTML・JavaScript・辞書を配信する静的ホスティングです。GitHub上でモデルを常時推論させる構成ではありません。
- 「AIを準備する」を押したときだけ `@litert-lm/core@0.17.0` をjsDelivrから取得し、公式ドキュメントが案内する [Gemma 4 E2B LiteRTモデル](https://huggingface.co/litert-community/gemma-4-E2B-it-litert-lm/tree/main) の `gemma-4-E2B-it-web.litertlm`（2,008,432,640バイト、約2GB）をHugging Faceから取得します。モデルはPagesやリポジトリに格納しません。
- 相談内容の推論は閲覧者のブラウザ内で行います。外部AI APIの従量課金や運営者の推論用GPUは不要ですが、閲覧者のGPU・メモリ・通信・電力を使います。ダウンロード容量は実行時メモリの必要量と同じではありません。モデルを端末に保存する設定では、CacheStorageに約2GBを1つ保存し、次回は保存済みモデルを読み込みます。HTTPキャッシュへの別コピーを避けるためモデル取得は `cache: no-store` を指定します。ブラウザによるデータ消去や保存データの削除後は、再取得が必要です。
- WebGPUを利用できない環境ではAIだけ案内を表示し、検索と分析は利用できます。Androidの一部のChromeやiOS 26以降のSafariにもWebGPUがありますが、このモデルの動作・速度・必要メモリは端末ごとの実機確認が必要です。AI相談はPC推奨と表示し、スマホではメモリ不足等で生成できない場合があることを案内します。ブラウザ版API自体もearly previewです。
- 最大8,192トークンの範囲で相談し、入力は1,200文字、回答は1,200トークンに制限します。現在の会話長と次の入力の保守的な上限を確認し、残り容量が不足する場合は新しい相談を案内します。停止・やり直し・終了に対応し、モデルと会話の生成中に終了した場合も完了を待って破棄します。
- JavaScriptのテストでは実際の辞書検索・統計と、SDKを差し替えた読み込み／中断／失敗／候補表示を検証します。これらのテストは実GPUによるGemmaの回答品質・速度やスマホの実機試験の代替ではありません。

```sh
python -m unittest discover -s tests
node --experimental-vm-modules --test tests/*.cjs
```


## 公式Vライバー名簿の追加収集

2026年9月の追加更新では、次の3情報源から903件を新規追加しました。総収録は32,345件、本人・所属先の根拠がある主な活動媒体は1,050件です（更新時点）。

- [Razzプロダクション](https://razz.jp/liver): 公開ページが使用する名簿データから646件。公式のデビュー日が過去にあり、本人のIRIAM識別子を確認できるものに限定します。公式の読み・英字も取り込みます。
- [Li:start](https://listart.jp/): 公開ページが配信表示に使用するデータから、正の月間配信時間または実際の配信中状態を確認できた151件。ランク・フォロワー数だけでは追加しません。運営管理用の項目、画像、プロフィール全文は保存しません。
- [Atoms](https://kyampus.me/atoms/liver): 「More」の公開ページを含む名簿から個別プロフィールを確認し、108件がデビュー日・本人のREALITY配信先の条件を満たしました。2件は既存掲載者の補完、106件は新規です。個人プロフィールの範囲だけからアカウントを取り出し、事務所共通のSNSやフッターの配信先で人物を統合しません。

IRIAMの公式アプリURIの識別子も照合に利用します。UUIDからWeb共有トークンを作ることはせず、Webで開けるアカウントがなければ公式プロフィールの出典を案内します。Vライバーである根拠を `vliver_source` に保持し、検索結果に「Vライバー」を表示します。既存のAIVTuber分類と確認済みの読みを保持します。

`directory_sources.py` を日次の媒体更新に組み込みました。RazzとLi:startは公開名簿を毎日照合し、Atomsは全ページを確認したうえで個人プロフィール40件ずつを巡回します。取得不能時は既存の掲載を保持します。初回調査では `--bulk-only --full --cache-dir <調査用フォルダ>` で追加名簿だけを取得・再検証できます。調査キャッシュは公開ファイルに含めません。

## AI保存と相談画面の改善

- `ai-model.js` がモデルの取得・保存・再利用・削除を担当します。モデルは公式リポジトリのリビジョン `b3ca0d2f076785a8f4b2219ddbd2bdb99954eae1` に固定しています。
- 空き容量を確認してから保存し、2GBのレスポンスをメモリ上に複製せず直接保存します。保存が完了してから開き直して推論エンジンへ渡します。不完全な保存は削除し、自動再ダウンロードで通信量を増やしません。Web Locks対応環境では複数タブの取得も調整します。
- 同じモデルを起動するたびにコピーは増えません。「AIを終了」は実行資源を解放し、保存モデルを残します。「保存したAIを削除」はAIを終了して本サイトのモデル保存だけを削除します。保存しない利用も選べます。
- 自由な文章の回答や、JSONの途中まで届いた場合でも取り出せる回答本文を表示します。カードにできた候補だけ実際に辞書で自動照合し、通常文の回答では名前検索での確認を案内します。空の回答・生成の失敗・保存の失敗を区別します。
- 相談画面にロボットの案内役を加え、会話とモデルの準備を分けました。`site.css` と `naming.css` でスマホ向けの配置と文字サイズを調整しています。キャラクターは組み込みの画像生成で制作し、`assets/naming-robot.png` に保存しました。生成プロンプトは `assets/naming-robot.prompt.txt` に記録しています。

ローカル検証は辞書・UIロジック・保存処理の自動テストです。約2GBの実モデルを使うGPU生成、実機での性能やモバイルのメモリ限界は別途確認が必要です。
