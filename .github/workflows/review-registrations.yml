name: Review registrations with Gemma
on:
  workflow_dispatch:
permissions: {}
jobs:
  discontinued:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Registration has been discontinued.'
